# public/_redirects
/*    /index.html   200
# This file is used by Netlify to redirect all requests to index.html
# This is necessary for single-page applications (SPAs) to handle routing correctly
# The above line means that any request to the root or any path will be served the index.html file